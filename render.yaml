services:
  - type: web
    name: vectordb
    runtime: python
    buildCommand: pip install uv && uv pip install -r requirements.txt --system
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT